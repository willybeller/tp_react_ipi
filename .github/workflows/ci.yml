name: Expo CI CD

on:
  push:
    branches: [main]
jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Install Node Modules
        run: npm install
        
      - name: view tree
        run: tree /home/runner/work

      - name: Test
        run: npm run test
